name: Publish to edge

on:
  workflow_dispatch:
  push:
    branches:
      - 5/main

jobs:
  publish-to-edge:
    uses: canonical/operator-workflows/.github/workflows/publish_charm.yaml@main
    secrets: inherit
    env:
      INPUT_BASE_BRANCH: 5/main
    with:
      channel: 5/edge
