# Copyright 2025 Canonical Ltd.
# See LICENSE file for licensing details.

name: Terraform modules release

on:
  push:
    branches:
      - main
    paths:
      - '**/terraform/**'

permissions:
  contents: write

jobs:
  terraform-release:
    uses: canonical/operator-workflows/.github/workflows/terraform_modules_release.yaml@main  # main
